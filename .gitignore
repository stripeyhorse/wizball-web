node_modules/

# Bun is the package manager here. A stray npm lockfile also makes Vercel's
# build detection pick npm over bun.
package-lock.json
yarn.lock
pnpm-lock.yaml

# Claude Code local state (contains worktrees / stale checkouts — never commit)
.claude/
.codex

# Editor / OS cruft
.DS_Store
*.swp
.idea/
.vscode/

# Vite
.vite/
*.local

# Build output — Vercel builds this, it is not committed
dist/

# Vercel CLI local state
.vercel/
